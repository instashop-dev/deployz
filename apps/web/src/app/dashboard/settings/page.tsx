import { SectionPlaceholder } from '@/components/section-placeholder';

export default function SettingsPage() {
  return (
    <SectionPlaceholder
      title="Settings"
      description="Your organization name and billing access will live here."
    />
  );
}
