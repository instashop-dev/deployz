import { SectionPlaceholder } from '@/components/section-placeholder';

export default function CustomersPage() {
  return (
    <SectionPlaceholder
      title="Customers"
      description="Add customers and generate their install links here."
    />
  );
}
