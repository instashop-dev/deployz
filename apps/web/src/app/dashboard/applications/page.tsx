import { SectionPlaceholder } from '@/components/section-placeholder';

export default function ApplicationsPage() {
  return (
    <SectionPlaceholder
      title="Applications"
      description="Your app's releases and compatibility checks will live here."
    />
  );
}
