import { defineConfig } from 'vitest/config';

// Standalone project, matching packages/contracts/vitest.config.ts — an empty
// local config keeps the repo-root workspace globs from finding nothing here.
export default defineConfig({});
